# name: Terraform Infrastructure

# on:
#   # Runs terraform plan + apply when PR is merged to main
#   push:
#     branches: [main]
#     paths:
#       - 'envs/dev/**'
#       - 'modules/**'

#   # Runs terraform plan on every PR — so you can review before merging
#   pull_request:
#     branches: [main]
#     paths:
#       - 'envs/dev/**'
#       - 'modules/**'

#   workflow_dispatch:
#     inputs:
#       environment:
#         description: 'Target environment'
#         required: true
#         default: 'dev'
#         type: choice
#         options: [dev, qa, prod]
#       # github.event.inputs.action used in job conditions below
#       action:
#         description: 'Terraform action'
#         required: true
#         default: 'plan'
#         type: choice
#         options: [plan, apply, destroy]
      
#       confirm_destroy:
#         description: 'Type "destroy" to confirm destruction (required when action=destroy)'
#         required: false
#         default: ''

# # This gives the GitHub Runner permission to Clone Repository
# permissions:
#   contents: read

# env:
#   TF_VERSION: '1.12.2'
#   AWS_REGION: us-east-1
#   TARGET_ENV: ${{ github.event.inputs.environment || 'dev' }}

# jobs:
#   ##############################################################
#   # JOB 1 — PLAN
#   # Always runs — on PR, on push to main, on manual trigger
#   # Shows exactly what will change before anything is applied
#   ##############################################################
#   plan:
#     name: Terraform Plan (${{ github.event.inputs.environment || 'dev' }})
#     runs-on: ubuntu-latest

#     # FIX: added job-level permissions — needed for OIDC token
#     # and posting plan output as PR comment
#     permissions:
#       id-token: write
#       contents: read
#       pull-requests: write

#     defaults:
#       run:
#         working-directory: envs/${{ github.event.inputs.environment || 'dev' }}

#     steps:
#       # Step 1 — Get the code
#       - name: Checkout code
#         uses: actions/checkout@v4

#       # Step 2 — Install Terraform
#       - name: Setup Terraform ${{ env.TF_VERSION }}
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: ${{ env.TF_VERSION }}

#       # Step 3 — Authenticate to AWS using static keys
#       # (OIDC for app repos is set up via IAM module —
#       # this workflow uses static keys since it IS the one
#       # creating the OIDC infrastructure)
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       # Step 4 — Resolve which env-specific secrets to use
#       # This is how one workflow handles dev/qa/prod without
#       # separate workflow files for each environment
#       - name: Resolve environment secrets
#         env:
#           DEV_DB_PASSWORD:  ${{ secrets.DEV_DB_PASSWORD }}
#           DEV_JWT_SECRET:   ${{ secrets.DEV_JWT_SECRET }}
#           QA_DB_PASSWORD:   ${{ secrets.QA_DB_PASSWORD }}
#           QA_JWT_SECRET:    ${{ secrets.QA_JWT_SECRET }}
#           PROD_DB_PASSWORD: ${{ secrets.PROD_DB_PASSWORD }}
#           PROD_JWT_SECRET:  ${{ secrets.PROD_JWT_SECRET }}
#         run: |
#           case "${{ env.TARGET_ENV }}" in
#             dev)
#               echo "TF_DB_PASSWORD=${DEV_DB_PASSWORD}" >> $GITHUB_ENV
#               echo "TF_JWT_SECRET=${DEV_JWT_SECRET}"   >> $GITHUB_ENV
#               ;;
#             qa)
#               echo "TF_DB_PASSWORD=${QA_DB_PASSWORD}" >> $GITHUB_ENV
#               echo "TF_JWT_SECRET=${QA_JWT_SECRET}"   >> $GITHUB_ENV
#               ;;
#             prod)
#               echo "TF_DB_PASSWORD=${PROD_DB_PASSWORD}" >> $GITHUB_ENV
#               echo "TF_JWT_SECRET=${PROD_JWT_SECRET}"   >> $GITHUB_ENV
#               ;;
#           esac

#       - name: Terraform Format Check
#         id: fmt
#         run: terraform fmt -check -recursive
#         continue-on-error: true

#       - name: Terraform Init
#         id: init
#         run: terraform init -backend-config=backend.tfvars

#       - name: Terraform Validate
#         id: validate
#         run: terraform validate -no-color

#       # Generate the plan and save it as a binary file
#       # -out=tfplan saves the exact plan so apply runs
#       # the same thing you reviewed — no surprises
#       - name: Terraform Plan
#         id: plan
#         run: |
#           terraform plan \
#             -var="aws_region=${{ env.AWS_REGION }}" \
#             -var="db_password=${{ env.TF_DB_PASSWORD }}" \
#             -var="jwt_secret=${{ env.TF_JWT_SECRET }}" \
#             -var="github_org=${{ vars.GH_ORG }}" \
#             -out=tfplan \
#             -no-color
#         continue-on-error: true

#       - name: Check Plan Status
#         if: steps.plan.outcome == 'failure'
#         run: exit 1

#       - name: Release state lock on failure
#         if: failure() || cancelled()
#         run: |
#           # FIX: changed VARS → vars (GitHub variables are lowercase)
#           aws s3 rm s3://${{ vars.TF_STATE_BUCKET }}/envs/${{ env.TARGET_ENV }}/terraform.tfstate.tflock || true

#       # Upload the plan binary so the apply job can use it
#       # This guarantees apply runs EXACTLY what plan showed
#       - name: Upload plan file
#         if: steps.plan.outcome == 'success'
#         uses: actions/upload-artifact@v4
#         with:
#           name: tfplan-${{ env.TARGET_ENV }}
#           path: envs/${{ env.TARGET_ENV }}/tfplan
#           retention-days: 1

#   ##############################################################
#   # JOB 2 — APPLY
#   # Only runs on push to main (not on PRs)
#   # Waits for manual approval via GitHub Environment gate
#   ##############################################################
#   apply:
#     name: Terraform Apply (${{ github.event.inputs.environment || 'dev' }})
#     runs-on: ubuntu-latest
#     needs: plan
#     if: |
#       (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
#       (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
#     environment: ${{ github.event.inputs.environment || 'dev' }}

#     permissions:
#       id-token: write
#       contents: read

#     # FIX: was "default:" → must be "defaults:"
#     defaults:
#       run:
#         working-directory: envs/${{ github.event.inputs.environment || 'dev' }}

#     steps:
#       - uses: actions/checkout@v4

#       - name: Setup Terraform ${{ env.TF_VERSION }}
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: ${{ env.TF_VERSION }}

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Terraform Init
#         run: terraform init -backend-config=backend.tfvars

#       - name: Download plan file
#         uses: actions/download-artifact@v4
#         with:
#           name: tfplan-${{ env.TARGET_ENV }}
#           path: envs/${{ env.TARGET_ENV }}

#       - name: Terraform Apply
#         run: |
#           # FIX: was "terrsform" → "terraform" (typo)
#           terraform apply \
#             -auto-approve \
#             -no-color \
#             tfplan

#       - name: Release state lock on failure
#         if: failure() || cancelled()
#         run: |
#           aws s3 rm s3://${{ vars.TF_STATE_BUCKET }}/envs/${{ env.TARGET_ENV }}/terraform.tfstate.tflock || true

#   ##############################################################
#   # JOB 3 — DESTROY
#   # Only runs on manual workflow_dispatch with action=destroy
#   # Requires typing "destroy" in the confirmation field
#   # Safety net so you never accidentally destroy prod
#   ##############################################################
#   destroy:
#     name: Terraform Destroy (${{ github.event.inputs.environment || 'dev' }})
#     runs-on: ubuntu-latest
#     if: |
#       github.event_name == 'workflow_dispatch' &&
#       github.event.inputs.action == 'destroy' &&
#       github.event.inputs.confirm_destroy == 'destroy'
#     environment: ${{ github.event.inputs.environment || 'dev' }}

#     permissions:
#       id-token: write
#       contents: read

#     defaults:
#       run:
#         working-directory: envs/${{ github.event.inputs.environment || 'dev' }}

#     steps:
#       - uses: actions/checkout@v4

#       - name: Setup Terraform ${{ env.TF_VERSION }}
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: ${{ env.TF_VERSION }}

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Resolve environment secrets
#         env:
#           DEV_DB_PASSWORD:  ${{ secrets.DEV_DB_PASSWORD }}
#           DEV_JWT_SECRET:   ${{ secrets.DEV_JWT_SECRET }}
#           QA_DB_PASSWORD:   ${{ secrets.QA_DB_PASSWORD }}
#           QA_JWT_SECRET:    ${{ secrets.QA_JWT_SECRET }}
#           PROD_DB_PASSWORD: ${{ secrets.PROD_DB_PASSWORD }}
#           PROD_JWT_SECRET:  ${{ secrets.PROD_JWT_SECRET }}
#         run: |
#           case "${{ env.TARGET_ENV }}" in
#             dev)
#               echo "TF_DB_PASSWORD=${DEV_DB_PASSWORD}"   >> $GITHUB_ENV
#               echo "TF_JWT_SECRET=${DEV_JWT_SECRET}"     >> $GITHUB_ENV
#               ;;
#             qa)
#               echo "TF_DB_PASSWORD=${QA_DB_PASSWORD}"    >> $GITHUB_ENV
#               echo "TF_JWT_SECRET=${QA_JWT_SECRET}"      >> $GITHUB_ENV
#               ;;
#             prod)
#               echo "TF_DB_PASSWORD=${PROD_DB_PASSWORD}"  >> $GITHUB_ENV
#               echo "TF_JWT_SECRET=${PROD_JWT_SECRET}"    >> $GITHUB_ENV
#               ;;
#           esac

#       - name: Terraform Init
#         run: terraform init -backend-config=backend.tfvars

#       - name: Terraform Destroy
#         run: |
#           terraform destroy \
#             -var="aws_region=${{ env.AWS_REGION }}" \
#             -var="db_password=${{ env.TF_DB_PASSWORD }}" \
#             -var="jwt_secret=${{ env.TF_JWT_SECRET }}" \
#             -var="github_org=${{ vars.GH_ORG }}" \
#             -auto-approve \
#             -no-color

#       - name: Release state lock on failure
#         if: failure() || cancelled()
#         run: |
#           aws s3 rm s3://${{ vars.TF_STATE_BUCKET }}/envs/${{ env.TARGET_ENV }}/terraform.tfstate.tflock || true


name: Terraform Infrastructure

# Push/PR to main on envs/dev/** or modules/** → auto plan+apply for dev
# Manual dispatch → choose environment (dev / qa / prod) + action (plan / apply / destroy)

on:
  push:
    branches: [main]
    paths:
      - 'envs/dev/**'
      - 'modules/**'
  pull_request:
    branches: [main]
    paths:
      - 'envs/dev/**'
      - 'modules/**'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'dev'
        type: choice
        options: [dev, qa, prod]
      action:
        description: 'Terraform action'
        required: true
        default: 'plan'
        type: choice
        options: [plan, apply, destroy]
      confirm_destroy:
        description: 'Type "destroy" to confirm destruction (required when action=destroy)'
        required: false
        default: ''

permissions:
  contents: read

env:
  TF_VERSION: '1.10.0'
  AWS_REGION: us-east-1
  # For push/PR auto-triggers, target is always dev.
  # For workflow_dispatch, use the selected environment.
  TARGET_ENV: ${{ github.event.inputs.environment || 'dev' }}

# Prevent parallel runs on the same branch — avoids state conflicts
concurrency:
  group: terraform-${{ github.ref }}-${{ github.event.inputs.environment || 'dev' }}
  cancel-in-progress: false

jobs:
  # ── Plan ──────────────────────────────────────────────────────────────────
  plan:
    name: Terraform Plan (${{ github.event.inputs.environment || 'dev' }})
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: envs/${{ github.event.inputs.environment || 'dev' }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform ${{ env.TF_VERSION }}
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Resolve environment secrets
        env:
          DEV_DB_PASSWORD:  ${{ secrets.DEV_DB_PASSWORD }}
          DEV_JWT_SECRET:   ${{ secrets.DEV_JWT_SECRET }}
          QA_DB_PASSWORD:   ${{ secrets.QA_DB_PASSWORD }}
          QA_JWT_SECRET:    ${{ secrets.QA_JWT_SECRET }}
          PROD_DB_PASSWORD: ${{ secrets.PROD_DB_PASSWORD }}
          PROD_JWT_SECRET:  ${{ secrets.PROD_JWT_SECRET }}
        run: |
          case "${{ env.TARGET_ENV }}" in
            dev)
              echo "TF_DB_PASSWORD=${DEV_DB_PASSWORD}"   >> $GITHUB_ENV
              echo "TF_JWT_SECRET=${DEV_JWT_SECRET}"     >> $GITHUB_ENV
              ;;
            qa)
              echo "TF_DB_PASSWORD=${QA_DB_PASSWORD}"    >> $GITHUB_ENV
              echo "TF_JWT_SECRET=${QA_JWT_SECRET}"      >> $GITHUB_ENV
              ;;
            prod)
              echo "TF_DB_PASSWORD=${PROD_DB_PASSWORD}"  >> $GITHUB_ENV
              echo "TF_JWT_SECRET=${PROD_JWT_SECRET}"    >> $GITHUB_ENV
              ;;
          esac

      - name: Terraform Format Check
        id: fmt
        run: terraform fmt -check -recursive
        continue-on-error: true

      - name: Terraform Init
        id: init
        run: terraform init -backend-config=backend.tfvars -reconfigure

      - name: Terraform Validate
        id: validate
        run: terraform validate -no-color

      - name: Terraform Plan
        id: plan
        run: |
          terraform plan \
            -var="aws_region=${{ env.AWS_REGION }}" \
            -var="db_password=${{ env.TF_DB_PASSWORD }}" \
            -var="jwt_secret=${{ env.TF_JWT_SECRET }}" \
            -var="github_org=${{ vars.GH_ORG }}" \
            -out=tfplan \
            -no-color
        continue-on-error: true

      - name: Check plan status
        if: steps.plan.outcome == 'failure'
        run: exit 1

      - name: Release state lock on failure
        if: failure() || cancelled()
        run: |
          aws s3 rm s3://${{ vars.TF_STATE_BUCKET }}/envs/${{ env.TARGET_ENV }}/terraform.tfstate.tflock || true

      - name: Upload plan file
        if: steps.plan.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-${{ env.TARGET_ENV }}
          path: envs/${{ env.TARGET_ENV }}/tfplan
          retention-days: 1

  # ── Apply ─────────────────────────────────────────────────────────────────
  apply:
    name: Terraform Apply (${{ github.event.inputs.environment || 'dev' }})
    runs-on: ubuntu-latest
    needs: plan
    if: |
      (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
    environment: ${{ github.event.inputs.environment || 'dev' }}
    defaults:
      run:
        working-directory: envs/${{ github.event.inputs.environment || 'dev' }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform ${{ env.TF_VERSION }}
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Terraform Init
        run: terraform init -backend-config=backend.tfvars -reconfigure

      - name: Download plan file
        uses: actions/download-artifact@v4
        with:
          name: tfplan-${{ env.TARGET_ENV }}
          path: envs/${{ env.TARGET_ENV }}

      - name: Terraform Apply
        run: |
          terraform apply \
            -auto-approve \
            -no-color \
            tfplan

      - name: Release state lock on failure
        if: failure() || cancelled()
        run: |
          aws s3 rm s3://${{ vars.TF_STATE_BUCKET }}/envs/${{ env.TARGET_ENV }}/terraform.tfstate.tflock || true

  # ── Destroy ───────────────────────────────────────────────────────────────
  destroy:
    name: Terraform Destroy (${{ github.event.inputs.environment || 'dev' }})
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'workflow_dispatch' &&
      github.event.inputs.action == 'destroy' &&
      github.event.inputs.confirm_destroy == 'destroy'
    environment: ${{ github.event.inputs.environment || 'dev' }}
    defaults:
      run:
        working-directory: envs/${{ github.event.inputs.environment || 'dev' }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform ${{ env.TF_VERSION }}
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Resolve environment secrets
        env:
          DEV_DB_PASSWORD:  ${{ secrets.DEV_DB_PASSWORD }}
          DEV_JWT_SECRET:   ${{ secrets.DEV_JWT_SECRET }}
          QA_DB_PASSWORD:   ${{ secrets.QA_DB_PASSWORD }}
          QA_JWT_SECRET:    ${{ secrets.QA_JWT_SECRET }}
          PROD_DB_PASSWORD: ${{ secrets.PROD_DB_PASSWORD }}
          PROD_JWT_SECRET:  ${{ secrets.PROD_JWT_SECRET }}
        run: |
          case "${{ env.TARGET_ENV }}" in
            dev)
              echo "TF_DB_PASSWORD=${DEV_DB_PASSWORD}"   >> $GITHUB_ENV
              echo "TF_JWT_SECRET=${DEV_JWT_SECRET}"     >> $GITHUB_ENV
              ;;
            qa)
              echo "TF_DB_PASSWORD=${QA_DB_PASSWORD}"    >> $GITHUB_ENV
              echo "TF_JWT_SECRET=${QA_JWT_SECRET}"      >> $GITHUB_ENV
              ;;
            prod)
              echo "TF_DB_PASSWORD=${PROD_DB_PASSWORD}"  >> $GITHUB_ENV
              echo "TF_JWT_SECRET=${PROD_JWT_SECRET}"    >> $GITHUB_ENV
              ;;
          esac

      - name: Terraform Init
        run: terraform init -backend-config=backend.tfvars -reconfigure

      - name: Terraform Destroy
        run: |
          terraform destroy \
            -var="aws_region=${{ env.AWS_REGION }}" \
            -var="db_password=${{ env.TF_DB_PASSWORD }}" \
            -var="jwt_secret=${{ env.TF_JWT_SECRET }}" \
            -var="github_org=${{ vars.GH_ORG }}" \
            -auto-approve \
            -no-color

      - name: Release state lock on failure
        if: failure() || cancelled()
        run: |
          aws s3 rm s3://${{ vars.TF_STATE_BUCKET }}/envs/${{ env.TARGET_ENV }}/terraform.tfstate.tflock || true